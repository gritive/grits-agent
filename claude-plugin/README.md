# Grits Plugin for Claude Code

## Install

```bash
claude plugin add github:gritive/grits-agent/claude-plugin
```

## Prerequisites

1. Install Grits CLI: `brew tap gritive/tap && brew install grits`
2. Authenticate: `grits login`
